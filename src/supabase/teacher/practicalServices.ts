import { supabase } from "@/lib/supabase";

const addPractical = async ({ title, description, isCodeRequired, subject_id, teacherId, }) => {
    try {

    } catch (error) {

    }
}

const editPractical = async ({ }) => { }

const deletePractical = async ({ }) => {
}

const getPractical = async () => { }

const getPracticals = async () => {

}